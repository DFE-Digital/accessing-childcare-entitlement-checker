name: PR Checks

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json

      - name: Restore
        run: dotnet restore AccessingChildcareEntitlementChecker.slnx --locked-mode

      - name: dotnet format
        run: dotnet format AccessingChildcareEntitlementChecker.slnx --verify-no-changes --no-restore

  verify:
    name: Build, Tests & SonarCloud
    permissions:
      contents: read
      checks: write
      pull-requests: read
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: |
            **/packages.lock.json

      - name: Cache SonarCloud packages
        uses: actions/cache@v5.0.3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar-packages

      - name: Cache SonarCloud scanner
        id: cache-sonar-scanner
        uses: actions/cache@v5.0.3
        with:
          path: ./.sonar/scanner
          key: ${{ runner.os }}-sonar-scanner

      - name: Install SonarCloud scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        run: dotnet tool update dotnet-sonarscanner --tool-path ./.sonar/scanner

      - name: Begin SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./.sonar/scanner/dotnet-sonarscanner begin \
            /k:DFE-Digital_accessing-childcare-entitlement-checker \
            /o:dfe-digital \
            /d:sonar.host.url="https://sonarcloud.io" \
            /d:sonar.token="$SONAR_TOKEN" \
            /d:sonar.qualitygate.wait=true \
            /d:sonar.dotnet.excludeTestProjects=true \
            /d:sonar.cs.opencover.reportsPaths="TestResults/unit/**/coverage.opencover.xml" \
            /d:sonar.coverage.exclusions="**/Program.cs,**/*.html,**/*.json,**/*.scss,**/wwwroot/**" \
            /d:sonar.scanner.scanAll=false

      - name: Restore
        run: dotnet restore AccessingChildcareEntitlementChecker.slnx --locked-mode

      - name: Build (Release)
        run: dotnet build AccessingChildcareEntitlementChecker.slnx --configuration Release --no-restore

      - name: Unit tests + collect coverage
        run: >
          dotnet test tests/AccessingChildcareEntitlementChecker.UnitTests/AccessingChildcareEntitlementChecker.UnitTests.csproj
          --configuration Release
          --no-build
          --results-directory ./TestResults/unit
          --logger "trx;LogFileName=unit.trx"
          --collect:"XPlat Code Coverage"
          -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format="opencover,cobertura"

      - name: End SonarCloud analysis
        if: always()
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./.sonar/scanner/dotnet-sonarscanner end /d:sonar.token="$SONAR_TOKEN"

      - name: Publish unit test results
        if: always()
        uses: dorny/test-reporter@3d76b34a4535afbd0600d347b09a6ee5deb3ed7f # v2.6.0
        with:
          name: Tests (Unit) - Details
          path: TestResults/unit/*.trx
          reporter: dotnet-trx
          fail-on-error: true

      - name: Upload test + coverage outputs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: testresults-unit
          if-no-files-found: error
          path: TestResults/unit/**

      - name: Upload built outputs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: build-output
          if-no-files-found: error
          path: |
            artifacts/bin/**

      - name: Publish web app
        run: dotnet publish src/AccessingChildcareEntitlementChecker.Web/AccessingChildcareEntitlementChecker.Web.csproj --no-build --configuration Release --output publish

      - name: Zip publish output
        run: |
          cd publish
          zip -r ../webapp.zip .

      - name: Upload web app artifact
        uses: actions/upload-artifact@v7
        with:
          name: webapp-package
          path: webapp.zip

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: verify
    env:
      MIN_LINE_COVERAGE: "5"
    steps:
      - name: Download unit test results
        uses: actions/download-artifact@v8
        with:
          name: testresults-unit
          path: TestResults/unit

      - name: Generate coverage report and gate
        uses: danielpalme/ReportGenerator-GitHub-Action@cf6fe1b38ed5becc89ffe056c1f240825993be5b # v5.5.4
        with:
          reports: "TestResults/unit/**/coverage.opencover.xml"
          targetdir: "coveragereport"
          reporttypes: "HtmlInline;MarkdownSummaryGithub;TextSummary"
          customSettings: "minimumCoverageThresholds:lineCoverage=${{ env.MIN_LINE_COVERAGE }}"

      - name: Add coverage to job summary
        run: cat coveragereport/SummaryGithub.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coveragereport
          path: coveragereport

 deploy:
    name: Deploy
    needs: verify
    # TODO: review when deciding deployment strategy/ephemeral environments
    # remove feature branch before merge
    if: |
      (github.event_name == 'push' && github.ref_name == 'main') ||
      (github.event_name == 'pull_request' && github.head_ref == 'feature/hk-ac-485-fed-cred') ||
      (github.event_name == 'workflow_dispatch' && github.ref_name == 'feature/hk-ac-485-fed-cred')
    
    uses: ./.github/workflows/infra-deploy.yml
    with:
      # MANDATORY: You must pass the environment here because it is marked 'required: true' in infra-deploy.yml
      # Logic: If it's main, we might want 'test', otherwise 'development'
      environment: ${{ (github.ref_name == 'main' || inputs.environment == 'test') && 'test' || 'development' }}
    secrets: inherit
