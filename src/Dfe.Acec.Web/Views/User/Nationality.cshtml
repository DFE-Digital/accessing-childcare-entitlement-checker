@using Dfe.Acec.Web.Models.User
@using Microsoft.AspNetCore.Mvc.Localization
@inject Microsoft.Extensions.Localization.IStringLocalizer<SharedResources> Shared
@inject IViewLocalizer Localizer
@model NationalityViewModel

@{
    ViewData["Title"] = Html.DisplayNameFor(model => model.NationalityOptions);
}

@section BeforeContent {
    <govuk-back-link href="@Model.BackLink">
        @Shared["Button_Back"]
    </govuk-back-link>
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
        <h1 class="govuk-heading-l">
            @Html.DisplayNameFor(model => model.NationalityOptions)
        </h1>

        <p class="govuk-body">
            @Localizer["This helps us give you more accurate results."]
        </p>
        <p class="govuk-body">
            @Localizer["Your answers will not be shared and will not affect your immigration status, benefits, funding or any applications."]
        </p>

        <form method="post" novalidate>
            <input type="hidden" asp-for="ReturnTo" />

            <app-checkboxes for="NationalityOptions" />

            <govuk-button type="submit">
                @Shared["Button_Continue"]
            </govuk-button>
        </form>
    </div>
</div>
